{
  "name": "review-boilerplate",
  "version": "2.0.0",
  "description": "Re:VIEW boilerplate",
  "scripts": {
    "test": "gulp redpen",
    "setup": "bundle install --path vendor/bundle && npm install && ./bin/setup-redpen.sh",
    "deploy": "gulp deploy",
    "clean": "gulp clean",
    "pdf": "gulp pdf",
    "web": "gulp web"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:kubosho/review-boilerplate.git"
  },
  "keywords": [
    "book",
    "re:view"
  ],
  "author": "kubosho",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "del": "^2.2.1",
    "gulp": "^3.9.1"
  }
}
